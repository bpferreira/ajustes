name: Ajustes Diário (debug)

on:
  workflow_dispatch:

permissions:
  contents: write

defaults:
  run:
    shell: bash

jobs:
  baixar-ajustes:
    runs-on: ubuntu-latest
    container:
      image: bpferreira/ajustes:v1

    steps:
      - name: Checkout do repositório (forçar versão mais recente)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Força fetch completo para evitar cache

      - name: Verificar versão atual (sem cache)
        run: |
          echo "🔍 VERIFICAÇÃO DE CACHE:"
          echo "📅 Data/hora: $(date)"
          echo "📋 Último commit: $(git log -1 --oneline)"
          echo "📋 Commit hash: $(git rev-parse HEAD)"
          echo "📁 Conteúdo do workflow atual:"
          cat .github/workflows/ajustes.yml | head -10
          echo "✅ Verificação de cache concluída"

      - name: Fix Git ownership (system-level safe.directory)
        working-directory: /
        run: |
          git config --system --add safe.directory '*'
          git config --system --add safe.directory "$GITHUB_WORKSPACE"
          git config --global --add safe.directory "$GITHUB_WORKSPACE"
          git config --global user.name  "GitHub Action"
          git config --global user.email "action@github.com"

      - name: Echo env e diretórios
        run: |
          echo "GITHUB_WORKSPACE=$GITHUB_WORKSPACE"
          echo "PWD (bash)="; pwd
          echo "Listando raiz do repo:"; ls -la
          echo "R getwd():"; Rscript -e 'cat(getwd(), "\n")'

      - name: Garantir pastas de saída
        run: |
          mkdir -p data/ajustes/b3
          mkdir -p data/ajustes/anbima
          ls -la data/ajustes || true

      - name: Rodar baixar_b3.R (logando caminho)
        run: |
          echo "R getwd() antes do script:"; Rscript -e 'cat(getwd(), "\n")'
          Rscript R/baixar_b3.R
          echo "📂 Após baixar_b3.R, conteúdo data/ajustes/b3:"; ls -la data/ajustes/b3 || true

      - name: Rodar baixar_anbima.R (logando caminho)
        run: |
          echo "R getwd() antes do script:"; Rscript -e 'cat(getwd(), "\n")'
          Rscript R/baixar_anbima.R
          echo "📂 Após baixar_anbima.R, conteúdo data/ajustes/anbima:"; ls -la data/ajustes/anbima || true

      - name: Varredura — procurar arquivos gerados no workspace
        run: |
          echo "🔎 Procurando .csv/.xls no workspace..."
          find "$GITHUB_WORKSPACE" -type f \( -name "*.csv" -o -name "*.xls" \) -printf '%TY-%Tm-%Td %TT %p\n' | sort || true

      - name: Varredura ampla — se salvou fora do workspace
        run: |
          echo "🔎 Procurando .csv/.xls fora também (limitado)..."
          find / -maxdepth 4 -type f \( -name "*.csv" -o -name "*.xls" \) 2>/dev/null | grep -E '/(ajustes|anbima|b3)/' || true

      - name: Checar se o Git está ignorando algo
        run: |
          echo "Conteúdo do .gitignore (se houver):"
          [ -f .gitignore ] && cat .gitignore || echo "(sem .gitignore)"
          echo "--- git check-ignore ---"
          git check-ignore -v data/ajustes/* data/ajustes/b3/* data/ajustes/anbima/* || echo "Nada ignorado"

      - name: Status do repo
        run: |
          git status
